# specify path to momo.conf or leave blank
# default is the same as MomoCore
#momo_conf=/home/evan82/.momocore/momo.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
